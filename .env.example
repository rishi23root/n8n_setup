# ============================================
# n8n Docker Compose Environment Variables
# ============================================
# Copy this file to .env and fill in your values:
#   cp .env.example .env
#   nano .env  # or your preferred editor
# ============================================

# ============================================
# REQUIRED VARIABLES
# ============================================

# Domain Configuration
# Your main domain name (e.g., example.com)
DOMAIN_NAME=example.com

# Subdomain for n8n (e.g., n8n)
# Full URL will be: https://<SUBDOMAIN>.<DOMAIN_NAME>
SUBDOMAIN=n8n

# Let's Encrypt Email
# Email address for Let's Encrypt certificate notifications
EMAIL=your-email@example.com

# Database Password
# Strong password for PostgreSQL database
# IMPORTANT: Use a strong, unique password!
DB_POSTGRESDB_PASSWORD=change-me-to-secure-password

# ============================================
# OPTIONAL VARIABLES (with defaults)
# ============================================

# Database Configuration
# PostgreSQL username (default: n8n)
DB_POSTGRESDB_USER=n8n

# PostgreSQL database name (default: n8n)
DB_POSTGRESDB_DATABASE=n8n

# Timezone
# Timezone for n8n (e.g., America/New_York, Europe/London, Asia/Tokyo)
# Leave empty to use system default
# See: https://en.wikipedia.org/wiki/List_of_tz_database_time_zones
GENERIC_TIMEZONE=America/New_York

# External IP Address
# Your server's external IP address
# Leave empty to auto-detect (recommended)
# EXTERNAL_IP=

# ============================================
# NOTES
# ============================================
# - All variables can be unquoted or quoted (both work)
# - Comments (lines starting with #) are ignored
# - Empty lines are ignored
# - Inline comments are supported (everything after #)
# - Make sure DOMAIN_NAME DNS points to your server's IP
# - Ensure ports 80 and 443 are open and accessible
